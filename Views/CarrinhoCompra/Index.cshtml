@model CarrinhoCompraViewModel

<div class="container">
<div class="row">

    <div>
        <h4> Seu carrinho de compras:
        <img src="~/images/shoppingcart.png"/>
        </h4>
    </div>

    <table class="table table-bordered table-striped">

        <thead>
            <tr>
                <th>Quantidade</th>
                <th>Skin</th>
                <th class="text-right">Preço</th>
                <th class="text-right">Subtotal</th>
                <th></th>
            </tr>
        </thead>

        <tbody>
            @foreach(var item in Model.CarrinhoCompra.CarrinhoCompraItems)
            {
                    <tr>
                        <td class="text-center">@item.Quantidade</td>
                        <td class="text-left">@item.Skin.Nome</td>
                        <td class="text-right">@item.Skin.Preco.ToString("c")</td>

                        <td>
                            @if (item.DescontoPreco.HasValue)
                            {
                                @($"{item.Quantidade * item.DescontoPreco:c} (-{(item.Quantidade * (item.Skin.Preco - item.DescontoPreco)):c})")
                            }
                            else
                            {
                                @($"{(item.Quantidade * item.Skin.Preco):c}")
                            }
                        </td>

                        <td class="text-center">
                            <a asp-controller="CarrinhoCompra" asp-action="RemoverItemDoCarrinhoCompra"
                               asp-route-skinId="@item.Skin.SkinId"><img src="~/images/deletar-lixeira.png" height="3%" width="3%"/>
                            </a>
                    </tr>
            }
        </tbody>
        <tfoot>
            <tr>
                <td colspan="3" class="text-right">Total: </td>
                <td class="text-right">
                    @Model.CarrinhoCompraTotal.ToString("c")
                </td>
            </tr>
        </tfoot>

    </table>

    <div class="text-right">
        <form asp-controller="CarrinhoCompra" asp-action="AplicarCupom" method="post">
            <input type="text" name="cupomCodigo" placeholder="Código do cupom">
            <button type="submit" class="btn btn-success">Aplicar Cupom</button>
        </form>
    </div>
    <br />

    <div class="text-right">
        <div class="btn-group">
            <a class="btn btn-success" asp-controller="Pedido" asp-action="Checkout">Encerrar Pedido</a>
            &nbsp;&nbsp;&nbsp;
            <a class="btn btn-primary" asp-controller="Skin" asp-action="List">Continuar Comprando</a>
        </div>
    </div>
</div>
</div>